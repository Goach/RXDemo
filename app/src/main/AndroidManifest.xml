<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.goach.rxjava">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RXJava">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.RXJava.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateActivity" />
        <activity android:name=".TransformActivity"/>
        <activity android:name=".FilterActivity"/>
        <activity android:name=".ConditionActivity"/>
        <activity android:name=".BooleanActivity"/>
        <activity android:name=".MergeActivity"/>
        <activity android:name=".ConnectActivity"/>
        <activity android:name=".ObservedActivity"/>
        <activity android:name=".SubjectActivity"/>
        <activity android:name=".RXLifeCycleActivity"/>
    </application>

</manifest>